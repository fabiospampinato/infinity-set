{
  "name": "infinity-set",
  "description": "A Set that doesn't throw if you put more than 16 million items in it. Because that's what the native `Set` object does for some reason.",
  "version": "1.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "compile": "tsc --skipLibCheck && tstei",
    "compile:watch": "tsc --skipLibCheck --watch",
    "prepublishOnly": "npm run clean && npm run compile"
  },
  "bugs": {
    "url": "https://github.com/fabiospampinato/infinity-set/issues"
  },
  "license": "MIT",
  "author": {
    "name": "Fabio Spampinato",
    "email": "spampinabio@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fabiospampinato/infinity-set.git"
  },
  "keywords": [
    "set",
    "infinite",
    "infinity",
    "limitless"
  ],
  "devDependencies": {
    "@types/node": "^15.3.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4",
    "typescript-transform-export-interop": "^1.0.4"
  }
}
